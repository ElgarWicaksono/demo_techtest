apiVersion: apps/v1
kind: Deployment
metadata:
  name: techtest-demo
  labels:
    app: springboot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: springboot
  template:
    metadata:
      labels:
        app: springboot
    spec:
      containers:
      - name: springboot
        image: elgarwicaksono/demo-techtest:version-12417f8e
        ports:
        - containerPort: 80